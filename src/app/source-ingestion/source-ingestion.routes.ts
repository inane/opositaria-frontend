import { Routes } from '@angular/router';
import { SourceIngestionComponent } from './infrastructure/ui/source-ingestion.component';

export const sourceIngestionRoutes: Routes = [{ path: '', component: SourceIngestionComponent }];
